import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        height: '100%',
        width: '100%',
        backgroundColor: '#1A1A1A',
        alignItems: 'center',
    },
    tasksContainer: {
        top: 40,
		flex: 1,
		width: '100%',
        height: '70%'
	},
})